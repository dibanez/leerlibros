{
  "name": "leerlibros",
  "version": "1.0.0",
  "private": true,
  "description": "Lector de libros en inglés con ayuda de vocabulario",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
